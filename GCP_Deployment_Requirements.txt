Subject: GCP Requirements for AI Bank Details Extractor App Deployment

Hi [Name],

I'm ready to deploy the AI Bank Details Extractor application to our company's GCP environment. To get this set up, I'll need the following access and resources:

**Project Setup:**
- Access to a GCP project (new or existing) with Owner or Editor permissions
- Billing enabled on the project

**APIs to Enable:**
- Cloud Run API
- Cloud Storage API  
- Firestore API
- Secret Manager API
- IAM Credentials API
- Cloud Build API
- Artifact Registry API

**Service Account:**
I need one service account created with these IAM roles:
- `roles/datastore.user` (database access)
- `roles/storage.admin` (file storage)
- `roles/secretmanager.secretAccessor` (app configuration)
- `roles/iam.serviceAccountTokenCreator` (file signing)
- `roles/logging.logWriter` (application logs)

**Additional Requirements:**
- Permission to create OAuth credentials (for user login functionality)
- Access to deploy Docker containers via gcloud CLI
- Ability to create Cloud Run services, Storage buckets, and Firestore databases

**Estimated Costs:** 
The app uses serverless architecture, so costs are minimal (~$5-30/month based on usage).

Once these are set up, the deployment is fully automated with a single script. Let me know when this is ready and I can proceed with the deployment.

Thanks!

[Your Name]
